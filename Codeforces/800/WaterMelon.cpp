#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    
    cin.tie(NULL);
    
    int n;
    
    cin>>n;
    
    if(n%2!=0)
    
    cout<<"NO"<<"\n";
    
    else
    {
        if(n==2)
            cout<<"NO"<<"\n";
            else
            cout<<"YES"<<"\n";
        }
    }   
    return 0;
}